// jika maouse nya digerakkan , maka warna akan berubah 
// tangkap elemen yang akan diubah warnanya
document.body.addEventListener('mousemove', function (e) {
  const xPos = Math.round((e.clientX /))
});